---
import BaseLayout from '../layouts/BaseLayout.astro';

const baseUrl = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`;
---

<BaseLayout title="404 - ページが見つかりません">
    <section style="padding: 6rem 2rem; text-align: center;">
        <div class="container">
            <h1 style="font-size: 4rem; margin-bottom: 1rem; color: var(--text-dark);">404</h1>
            <h2 style="font-size: 2rem; margin-bottom: 2rem; color: var(--text-light);">ページが見つかりません</h2>
            <p style="margin-bottom: 2rem; color: var(--text-light);">
                お探しのページは存在しないか、移動した可能性があります。
            </p>
            <a href={baseUrl} class="btn btn-primary">ホームに戻る</a>
        </div>
    </section>
</BaseLayout>
